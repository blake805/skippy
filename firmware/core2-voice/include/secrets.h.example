// Copy to secrets.h (gitignored) and fill in. The token must match the
// SKIPPY_VOICE_TOKEN the server was started with; leave "" if none is set.
#pragma once

#define WIFI_SSID    "your-network"
#define WIFI_PASS    "your-password"

#define SKIPPY_HOST  "192.168.1.151"  // the Mac Studio
#define SKIPPY_PORT  8000
#define SKIPPY_TOKEN ""
