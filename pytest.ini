[pytest]
testpaths = tests
# sample_repo ships its own suite; it is the agent's target, not ours.
addopts = --ignore=tests/fixtures -q
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
