[pytest]
testpaths = tests
# The sample repo ships its own test suite; it belongs to the agent, not to us.
addopts = --ignore=tests/fixtures -q
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
