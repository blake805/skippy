# units

Unit conversions for shop work. Run the tests with `python -m pytest -q` from
the repository root.

Conventions: public functions are typed, carry a one-line docstring, and are
re-exported from `units/__init__.py`.
