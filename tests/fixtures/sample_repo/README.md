# sample_repo

A deliberately tiny package used as the agent's target in tests. Copied into a
temp directory per test, so the agent may edit it freely.

Run its tests with `python3 -m pytest -q` from the repo root.
